#include <stdio.h>
#include <stdlib.h>

char	ft_join(int size, char **strs, char *sep)
{
	int	i;

	i = 0;
	*strs = (int *)malloc(sizeof(char) * size);
	while (i < size)
	{
		while ()
	}
}
int	main(void)
{
	int	size;
	char	strs[0] = "Hello";
	char	strs[1] = "mother";
	char	strs[2] = "fucker";
	char	sep = "ab";
	int	i;

	i = 0;
	while (i < size)
	{
		printf("%s", ft_strjoin(3, strs[i], sep));
	}
}
